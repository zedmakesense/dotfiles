# vim: ft=swayconfig

# exec clipse -listen &
exec wl-paste --type text --watch cliphist store &
exec wl-paste --type image --watch cliphist store &
exec wl-clip-persist --clipboard both --reconnect-tries 0 &

exec poweralertd &
exec mako &

# exec swaync &
exec playerctld daemon &

# arch
# exec /usr/bin/lxqt-policykit-agent &
# deb
exec /usr/bin/lxpolkit &
# nix
exec lxpolkit &

exec flatpak run com.github.wwmm.easyeffects --hide-window --service-mode
# exec docker start bentopdf omni-tools convertx &

exec wayland-pipewire-idle-inhibit --dry-run &
exec swayidle -w \
    timeout 180 'swaylock --ignore-empty-password --show-failed-attempts -f -c 000000' \
    timeout 180 'swaymsg "output * power off"' \
    resume 'swaymsg "output * power on"' \
    before-sleep 'swaylock --ignore-empty-password --show-failed-attempts -fFec 282828' &
